stages:
  - build
  - test

image: ultimaker/cura-build-environment

build and test:
  stage: build
  script: 
    - docker/build.sh
  artifacts:
    paths:
      - build

testUranium:
  stage: test
  script:
    - docker/test.sh


