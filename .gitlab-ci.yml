stages:
  - build
  - test

image: ultimaker/cura-build-environment

buildUranium:
  stage: build
  script: 
    - docker/build.sh
  artifacts:
    paths:
      - build

testUranium:
  stage: test
  script:
    - cd build
    - ctest3 --output-on-failure -T Test


